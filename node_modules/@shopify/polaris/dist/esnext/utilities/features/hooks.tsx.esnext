import { useContext } from 'react';
import { FeaturesContext } from './context.ts.esnext';

function useFeatures() {
  const features = useContext(FeaturesContext);

  if (!features) {
    throw new Error('No Features were provided.');
  }

  return features;
}

export { useFeatures };
