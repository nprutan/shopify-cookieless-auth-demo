import React from 'react';
interface SecondaryProps {
    expanded: boolean;
    children?: React.ReactNode;
}
export declare function Secondary({ children, expanded }: SecondaryProps): JSX.Element;
export {};
