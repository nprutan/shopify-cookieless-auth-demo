/// <reference types="react" />
import type { IconProps } from '../../types';
interface Props extends IconProps {
}
export declare function Icon({ source, color, backdrop, accessibilityLabel }: Props): JSX.Element;
export {};
