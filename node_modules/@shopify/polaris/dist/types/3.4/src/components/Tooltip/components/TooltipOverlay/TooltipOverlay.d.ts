import React from 'react';
import { PositionedOverlayProps } from '../../../PositionedOverlay';
export interface TooltipOverlayProps {
    id: string;
    active: boolean;
    light?: boolean;
    preventInteraction?: PositionedOverlayProps['preventInteraction'];
    preferredPosition?: PositionedOverlayProps['preferredPosition'];
    children?: React.ReactNode;
    activator: HTMLElement;
    accessibilityLabel?: string;
    onClose(): void;
}
export declare function TooltipOverlay({ active, activator, preferredPosition, preventInteraction, id, children, light, accessibilityLabel, }: TooltipOverlayProps): JSX.Element | null;
