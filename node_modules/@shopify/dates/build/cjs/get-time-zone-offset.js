"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getTimeZoneOffset = getTimeZoneOffset;

var _getDateTimeParts2 = require("./get-date-time-parts");

function getTimeZoneOffset() {
  var date = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : new Date();
  var timeZone1 = arguments.length > 1 ? arguments[1] : undefined;
  var timeZone2 = arguments.length > 2 ? arguments[2] : undefined;
  var date1 = constructZonedDateFromParts(date, timeZone1);
  var date2 = constructZonedDateFromParts(date, timeZone2);
  return (date1.valueOf() - date2.valueOf()) / (1000 * 60);
}

function constructZonedDateFromParts(date, timeZone) {
  var _getDateTimeParts = (0, _getDateTimeParts2.getDateTimeParts)(date, timeZone),
      year = _getDateTimeParts.year,
      month = _getDateTimeParts.month,
      day = _getDateTimeParts.day,
      hour = _getDateTimeParts.hour,
      minute = _getDateTimeParts.minute,
      second = _getDateTimeParts.second;

  return new Date(Date.UTC(year(), month() - 1, day(), hour(), minute(), second()));
}