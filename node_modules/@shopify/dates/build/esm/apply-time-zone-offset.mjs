import { getTimeZoneOffset } from './get-time-zone-offset';
export function applyTimeZoneOffset(date, timeZone1, timeZone2) {
  var initialOffset = getTimeZoneOffset(date, timeZone1, timeZone2);
  var adjustedDate = new Date(date.valueOf() - initialOffset * 60 * 1000);
  var targetOffset = getTimeZoneOffset(adjustedDate, timeZone1, timeZone2);
  var offsetDiff = targetOffset - initialOffset;
  return new Date(adjustedDate.valueOf() - offsetDiff * 60 * 1000);
}