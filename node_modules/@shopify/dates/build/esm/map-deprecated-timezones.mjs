import { deprecatedTimezones } from './deprecated-timezones';
export function mapDeprecatedTimezones(timezone) {
  if (Object.keys(deprecatedTimezones).some(function (key) {
    return key === timezone;
  })) {
    return deprecatedTimezones[timezone];
  } else {
    return timezone;
  }
}