import { getTimeZoneOffset } from './get-time-zone-offset';
export function unapplyTimeZoneOffset(date, timeZone1, timeZone2) {
  var timeZoneOffset = getTimeZoneOffset(date, timeZone1, timeZone2);
  return new Date(date.valueOf() + timeZoneOffset * 60 * 1000);
}