import { getDateTimeParts } from './get-date-time-parts';
import { isSameYear } from './is-same-year';
export function isSameMonth(date1, date2, timeZone) {
  var _getDateTimeParts = getDateTimeParts(date1, timeZone),
      month1 = _getDateTimeParts.month;

  var _getDateTimeParts2 = getDateTimeParts(date2, timeZone),
      month2 = _getDateTimeParts2.month;

  return isSameYear(date1, date2, timeZone) && month1() === month2();
}